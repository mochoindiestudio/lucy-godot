# Godot 4+ specific ignores
.godot/
/android/

# Windows atomic-write lock artifacts (left behind when a locked file
# can't be replaced in place, e.g. addon DLLs while Godot has them loaded)
~*.TMP
~*.tmp
